{
  "name": "gomr-vis-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "build:client": "cd client && npm run build",
    "start": "npm run build:client && cd ../server && npm run start",
    "dev": "concurrently \"cd client && npm run dev\" \"cd ../server && npm run dev\"",
    "serve": "cd client && npm run build && cd ../server && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
